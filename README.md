# bybite_bot

Bot scalping Bybit (spot & futures) pake EMA + RSI. Modal kecil friendly.

## Setup
1. Clone repo ini
2. `pip install -r requirements.txt`
3. Copy `.env.example` jadi `.env` → isi API key
4. Run salah satu:
   - python bot_monitor.py
   - python scalper_spot.py
   - python scalper_futures.py

Testnet: set TESTNET=true di .env

Warning: resiko tinggi, test dulu.